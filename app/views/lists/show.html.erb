<%= @list.name %>

<h2>Les films dans la liste</h2>
<% @list.movies.each do |movie| %>
<%= movie.title %>
<small class="text-right flex-shrink-0"><%= link_to "delete", bookmark_path(bookmark), data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to remove #{movie.title} from your #{@list.name} list"}, class: 'text-danger' %></small>
<% end %>

<h2>Ajoute un film</h2>

<button class = "btn btn-primary'"> </button>
<%= link_to 'Ajoute un film', new_list_bookmark_path(list_id: @list.id) %>

<h2>Supprime la liste</h2>
<%= link_to "Effacer", list_path(@list), class: "btn btn-cancel", data: { turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this list?" }  %>
